{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "node-terminal",
			"name": "Debug (tsx)",
			"request": "launch",
			"command": "pnpm tsx ${relativeFile}",
			"cwd": "${workspaceFolder}"
		}
	]
}
